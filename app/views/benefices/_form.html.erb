<%= form_with(model: benefice, local: true) do |form| %>
  <% if benefice.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(benefice.errors.count, "error") %> prohibited this benefice from being saved:</h2>

      <ul>
      <% benefice.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :estado %>
    <%= form.text_field :estado, id: :benefice_estado %>
  </div>

  <div class="field">
    <%= form.label :fecha_vencimiento %>
    <%= form.date_select :fecha_vencimiento, id: :benefice_fecha_vencimiento %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
